// Value → label map for priority <Select>s. Base UI shows the raw value in the
// trigger unless the label is provided through Select's `items` prop.
export const PRIORITY_LABELS = { urgent: "Urgent", high: "High", normal: "Normal", low: "Low" };

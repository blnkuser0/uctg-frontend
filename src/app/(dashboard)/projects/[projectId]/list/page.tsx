export default function ProjectListPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-2 p-8 text-center">
      <h1 className="text-sm font-semibold">List view</h1>
      <p className="max-w-sm text-sm text-muted-foreground">Arrives in a later milestone (task depth/visibility).</p>
    </div>
  );
}
